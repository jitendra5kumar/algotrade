/**
 * Strategy Monitor Service
 * 
 * Main export for the strategy monitoring service
 * This is a singleton instance that manages all strategy monitoring
 */

export { default } from "./strategy-monitor.service";
export type { MonitorState, HighLowReference, HighLowWatcherState } from "./types";

